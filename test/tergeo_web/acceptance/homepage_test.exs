defmodule Tergeo.Acceptance.HomepageTest do
  use TergeoWeb.AcceptanceCase

  test "when logged in I can see a list of my groups" do
    
  end

  test "when not logged in I see a link to start a group" do
    
  end

end