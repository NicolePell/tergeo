defmodule Tergeo.Acceptance.NavbarTest do
  use TergeoWeb.AcceptanceCase
  
  @moduletag :pending

  test "a user can navigate to their list of chores" do
  end

  test "a user can navigate to add a new chore" do
  end
  
  test "a user can navigate to their list of groups" do
  end
  
  test "a user can navigate to add a new group" do
  end
  
end
